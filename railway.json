{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --no-dev --optimize-autoloader && npm install && npm run build && php artisan key:generate"
    },
    "deploy": {
        "startCommand": "php artisan config:clear && php artisan cache:clear && php -S 0.0.0.0:$PORT public/index.php",
        "restartPolicy": "always"
    }
}