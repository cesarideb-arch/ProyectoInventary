{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --no-dev --optimize-autoloader && npm install && npm run build && php artisan key:generate && php artisan config:cache && php artisan route:cache && php artisan view:cache",
        "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
    },
    "deploy": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
        "restartPolicy": "on-failure"
    }
}